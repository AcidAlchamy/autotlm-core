# AutoTLM boards platform. Build recipes come from the referenced esp32 core
# (see boards.txt: one.build.core=esp32:esp32) — this file only names the
# platform for the IDE.
name=AutoTLM Boards
version=0.2.0
